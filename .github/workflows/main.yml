name: Node Setup
on:
    push:
          branches: [main]
jobs:
    setup-model:
      runs-on: ubuntu-latest

    defaults:
        run:
            working-directory: backend

    steps:
        - name: Checkout repo
          user: actions/checkout@v4

        - name: setup node
          uses: actions/setup-node@v4
